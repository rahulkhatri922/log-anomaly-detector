# Backend configuration (all optional — sane defaults for local use).

# Where SQLite DB + trained model are stored.
DATA_DIR=data

# Override the database (defaults to sqlite in DATA_DIR).
# DATABASE_URL=sqlite:///data/app.db
